<?xml version="1.0"?>
<block>
  <name>hier_rx_psk</name>
  <key>gwn_hier_rx_psk</key>
  <category>GWN</category>
  <import>import gwn</import>
  <make>gwn.hier_rx_psk($bw_clock_sync, $bw_fll, $bw_costas, $n_filts, $len_sym_srrc, $constellation, $1-1j, $1+1j, $-1+1j], $[], $4, $1).base(), $samp_per_sym, $alfa, $bits_per_sym, $agc_attack_rate, $agc_decay_rate, $agc_reference, $agc_gain, $alpha_probe, $th_probe)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>...</name>
    <key>...</key>
    <type>...</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type><!-- e.g. int, float, complex, byte, short, xxx_vector, ...--></type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type><!-- e.g. int, float, complex, byte, short, xxx_vector, ...--></type>
  </source>
</block>
